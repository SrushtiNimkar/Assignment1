// Print the ASCII value of user entered character in decimal, hex, octal 
// format and also print the character for user entered ASCII value.

#include <stdio.h>
int main()
{
  int main()
  printf("Enter the value")
    return 0;
}